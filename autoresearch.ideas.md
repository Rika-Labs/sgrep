- Explore graph-aware symbol-kind bonuses or container/module emphasis during reranking for locator-style code queries. Local symbol-token overlap helped, but avoid repeating the discarded approach of copying top-level file symbols into every chunk.
- Try field-specific abbreviation/acronym support for path and symbol terms without expanding all content text. The remaining `mmap_index` miss suggests compact identifiers can still be semantically opaque, but any aliasing should stay structural and general-purpose rather than hardcoded to repo terms.
- Consider separating lexical query-token expansion by retrieval stage: richer variants for BM25/BM25F recall, but more conservative tokens for PRF gating and any future intent classifiers. The current split between expanded lexical tokens and base PRF compactness worked well and may generalize to other ranking heuristics.
