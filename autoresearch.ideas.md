- Explore graph-aware symbol overlap or symbol-kind bonuses during reranking for locator-style code queries. The remaining misses (`bm25_keyword`, `json_output`, `mmap_index`, `search_thresholds`) still look like target-module discovery problems where symbol names may be more reliable than broad semantic similarity.
- Try field-specific abbreviation/acronym support for path and symbol terms without expanding all content text. The remaining `mmap_index` miss suggests compact identifiers can still be semantically opaque, but any aliasing should stay structural and general-purpose rather than hardcoded to repo terms.
